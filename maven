To install maven-------------------

#Pre requesties -------------
jdk
jre
sudo apt-get maven
sudo apt-get install maven

we have to environment variable in ~/.bash_profile

export M2_HOME=/opt/apache-maven-3.8.7
export PATH=$PATH:$M2_HOME/bin

Save & close
